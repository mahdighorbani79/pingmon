<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP to any IP — server address is dynamic, set via bot -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
